services:
  squat-bird-campus:
    build:
      context: .
    container_name: squat-bird-campus
    ports:
      - "4173:4173"
    environment:
      HOST: 0.0.0.0
      PORT: 4173
    restart: unless-stopped
